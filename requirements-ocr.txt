# OCR 模块专用依赖
# 
# ═══════════════════════════════════════════════════════════
# 状态: GPU 加速已启用 ✅
#   onnxruntime-gpu 1.21.0 + CUDA 12.x (Driver 610.47)
#   预处理: orientation + unwarping (essay 预设)
# ═══════════════════════════════════════════════════════════

# 基础依赖
paddleocr>=3.0.0

# GPU 加速 (CUDA 12.x 兼容)
# ⚠️ 不要升级到 1.27.0+，该版本需要 CUDA 13.x
onnxruntime-gpu>=1.20.0,<1.27.0
