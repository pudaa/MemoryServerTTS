# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.venv/
venv*/
.env

# VSCode
.vscode/
.vscode-test/
.github/

# Jupyter/Notebook
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Models & Data
models/
voices/
temp/

# Logs
*.log

# Others
*.bak
*.swp
.workbuddy/

# 提交信息草稿（历史上多次误入库：.cm5.txt / .cm7.txt / .commitmsg3.txt）
.cm*.txt
.commitmsg*.txt

# numba JIT 缓存（librosa 依赖；见 bench/README.md：目录不可写会导致 import qwen_tts 卡死）
.numba-cache/

# 基准脚本产出的试听样本（临时产物，不入库）
# 注意：bench/ab-out/ 只忽略音频与试听页，results.json / asr_wer.json 入库作为证据。
bench/out/
bench/out-*/
bench/ab-out/*.wav
bench/ab-out/index.html
# 对话场景评估：只留指标 JSON（verify.json），音频不入库
bench/conv-out/*.wav
bench/prof-out/
# 改造前的 perf 环境 pip freeze 快照（含本机绝对路径；环境信息见 docs/ENV_BACKUP.md）
bench/perf-env-before-faster.txt

# 运行时产出目录（服务启动后自动写入，不入库）
# tts-audio/：流式与合成输出音频（/tts-audio 静态挂载）；word-cache/：听写词库音频缓存
tts-audio/
word-cache/

# 测试脚本产出的音频
test_emotion_output.wav
